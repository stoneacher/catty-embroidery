# SwiftFormat configuration — applied by the PostToolUse hook and CI.
--swiftversion 6.0
--indent 4
--maxwidth 120
# SwiftLint's default trailing_comma rule forbids trailing commas; keep SwiftFormat from re-adding them.
--commas inline-only
--exclude **/.build, **/DerivedData
