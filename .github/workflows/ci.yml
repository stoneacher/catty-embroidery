name: CI

on:
  push:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  engine-tests:
    name: Engine tests (swift test)
    runs-on: macos-26
    env:
      # Pin the toolchain: runner-default Xcode drifts and breaks Swift 6.x builds.
      # Xcode 26.6 (Swift 6.3.3) matches the local development toolchain.
      DEVELOPER_DIR: /Applications/Xcode_26.6.app/Contents/Developer
    steps:
      - uses: actions/checkout@v5
      - name: Show toolchain
        run: swift --version
      # ADR-033: EditorCore depends on ProgramModel *only*, and is vended through
      # the existing ProgramModel product so the app needs no pbxproj change.
      #
      # This runs here rather than as a test, and the reason is measured rather
      # than stylistic: `dump-package` evaluates the manifest, which is the only
      # oracle that sees a dependency added after the initializer (a textual pin
      # on the declaration does not) -- but spawning it from inside `swift test`
      # deadlocks on SwiftPM's build lock; it blocked for 608 s before being
      # killed. Outside the test process there is no lock to contend for.
      #
      # ADR-023's division: EditorCoreTargetIsolationTests' textual pin is the
      # fast local convenience, this required status check is the enforcement.
      - name: Pin EditorCore's evaluated dependencies (ADR-033)
        run: |
          swift package --package-path Packages/EmbroideryEngine dump-package > /tmp/package.json
          python3 - <<'CHECK'
          import json, sys

          manifest = json.load(open("/tmp/package.json"))

          # The discriminator is checked, not flattened. `dump-package` encodes a
          # dependency as {"<kind>": [name, ...]}, and for a product the array is
          # [product, package, ...] -- so flattening every string made
          # `.product(name: "ProgramModel", package: "ProgramModel")` collapse to
          # {"ProgramModel"} and pass, while adding an external dependency
          # ADR-033 forbids (Codex round 3, reproduced before fixing).
          LOCAL_KINDS = {"byName", "target"}

          def local_dependency_names(entries):
              found, foreign = set(), []
              for entry in entries:
                  for kind, value in entry.items():
                      if kind in LOCAL_KINDS:
                          found.add(value[0])
                      else:
                          foreign.append(f"{kind}({value[0]})")
              return found, foreign

          target = next(t for t in manifest["targets"] if t["name"] == "EditorCore")
          deps, foreign = local_dependency_names(target["dependencies"])
          product = next(p for p in manifest["products"] if p["name"] == "ProgramModel")
          vended = set(product["targets"])

          failures = []
          if foreign:
              failures.append(f"EditorCore has non-local dependencies {sorted(foreign)}; ADR-033 permits the local ProgramModel target only")
          if deps != {"ProgramModel"}:
              failures.append(f"EditorCore depends on {sorted(deps)}, expected ['ProgramModel'] (ADR-033)")
          if vended != {"ProgramModel", "EditorCore"}:
              failures.append(f"ProgramModel product vends {sorted(vended)}, expected both targets (ADR-033)")

          for failure in failures:
              print(f"::error::{failure}")
          sys.exit(1 if failures else 0)
          CHECK

      - name: Run engine tests
        run: swift test --package-path Packages/EmbroideryEngine

  app-build-and-test:
    name: App build & test (xcodebuild)
    runs-on: macos-26
    # A wedged simulator would otherwise burn the 360-minute default. The healthy
    # job is ~4m30s, so this is generous while still failing in a useful time.
    timeout-minutes: 20
    env:
      # Same pin and the same reason as the engine job: the runner-default Xcode
      # drifts and breaks Swift 6.x builds. Xcode 26.6 (Swift 6.3.3) matches the
      # local development toolchain.
      DEVELOPER_DIR: /Applications/Xcode_26.6.app/Contents/Developer
    steps:
      - uses: actions/checkout@v5
      - name: Show toolchain
        run: xcodebuild -version
      - name: Require the shared scheme
        # Checked explicitly so a missing scheme fails here with a readable
        # message rather than inside xcodebuild. xcshareddata/xcschemes/ must be
        # committed: .gitignore excludes xcuserdata/, so a fresh checkout has no
        # scheme unless it is shared, and Xcode's autocreation cannot be relied on.
        run: |
          test -f catrobat_embroidery_ios/catrobat_embroidery_ios.xcodeproj/xcshareddata/xcschemes/catrobat_embroidery_ios.xcscheme \
            || { echo "::error::Shared scheme missing from xcshareddata/xcschemes/"; exit 1; }
      - name: Show available simulators
        run: xcrun simctl list devices available
      - name: Build and test the app target
        # -project is explicit, and must stay so: the project is nested and
        # Actions starts at the repository root, where a bare -scheme finds
        # nothing and fails before reaching a test.
        #
        # OS=latest is explicit too: a runner image carrying two iOS runtimes has
        # two devices named "iPhone 17", and xcodebuild picks one silently. That
        # is a coin flip the day the image ships a second runtime, so the choice
        # is stated rather than inherited.
        run: |
          xcodebuild test \
            -project catrobat_embroidery_ios/catrobat_embroidery_ios.xcodeproj \
            -scheme catrobat_embroidery_ios \
            -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' \
            -resultBundlePath "$RUNNER_TEMP/AppTests.xcresult"
      - name: Upload result bundle
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: app-tests-xcresult
          path: ${{ runner.temp }}/AppTests.xcresult

  lint:
    name: SwiftLint
    runs-on: ubuntu-latest
    container: ghcr.io/realm/swiftlint:0.65.0
    steps:
      - uses: actions/checkout@v5
      - name: Lint
        run: swiftlint --strict --reporter github-actions-logging
